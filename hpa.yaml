apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: minikube_java_app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: minikube_java_app-app
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50